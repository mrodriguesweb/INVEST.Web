@* @model TipoIndicadorViewModel

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

<button type="button" class="btn btn-primary">
    <a asp-controller="TipoIndicador" asp-action="PrepareCreate" style="color: #FFF;">Registrar Novo</a>
</button>

<table id="tableAcoes" class="display">
    <thead>
        <tr>
            <th>ID</th>
            <th>NOME</th>
            <th>TIPO</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var t in Model.TiposIndic)
        {
            <tr>
                <td>@t.Id</td>
                <td>@t.Name</td>
                <td>@t.</td>
                <td>...</td>
            </tr>
        }

    </tbody>
</table>

@section Scripts {
    <script src="~/js/acao.js" asp-append-version="true"></script>
} *@